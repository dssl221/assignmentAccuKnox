Q.1> By default, django signals are executed synchronously the signal handlers will be executed in the same process and block further code execution until they are completed.
Q.2> Yes, by default, django signals run in the same thread as the caller.
Q.3> Yes, by default, django signals run within the same database transaction as the caller.